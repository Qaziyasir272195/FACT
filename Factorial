#include<stdio.h>
    long int a (int n);
    int main() {
        int n;
        printf("enter a number");
        scanf("%d",&n);
        printf("fact of %d=%ld",n,a(n));
        return 0;
    }
    long int a (int n){
    if(n>=1)
        return n*a(n-1);
    else
        return 1;
}
